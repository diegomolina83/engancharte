<h1>Editar Usuario</h1>
<img class="userEditImage" src="{{imageUrl}}" alt="">

<div class="row justify-content-center">
    <div class="col-md-6">

        <h1>Registro</h1>

        <form action="/users/edit/{{_id}}" method="post" enctype="multipart/form-data">
            <div class="form-group">
                <label for="field1">Nombre de usuario</label>
                <input name="username" type="text" class="form-control" id="field1" value="{{username}}">
            </div>
            <div class="form-group">
                <label for="field2">Email</label>
                <input name="email" type="email" class="form-control" id="field2" value="{{email}}">
            </div>
             <div class="form-group">
                <label for="field2">Cambiar imagen</label>
                <input name="imageInput" type="file" class="form-control" id="field3" value="{{imageInput}}">
                <small class="form-text text-muted">El tamaño de la imagen</small>

            </div>
            <div class="form-group">
                <label for="field2">Contraseña</label>
                <input name="password" type="password" class="form-control" id="field4" value="****" >
                <small class="form-text text-muted">Nunca compartas tu contraseña</small>
            </div>

            <button type="submit" class="btn btn-dark btn-block btn-lg">Registrarme</button>

            {{#if errorMsg}}
            <div id="errorMsg">{{{errorMsg}}}</div>
            {{/if}}
        </form>

        <p>¿Tienes cuenta? <a href="/login">¡Inicia sesión!</a></p>
    </div>
</div>