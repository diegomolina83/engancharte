<h1>{{username}}</h1>
<img class="userEditImage" src="{{imageUrl}}" alt="">

<div class="row justify-content-center">
    <div class="col-md-6">

        <h1>Modifica tu perfil</h1>

        <form action="/users/edit/{{_id}}" method="post" enctype="multipart/form-data">
            <div class="form-group">
                <label for="field1">Nombre de usuario</label>
                <input name="username" type="text" class="form-control" id="field1" value="{{username}}">
            </div>
            <div class="form-group">
                <label for="field2">Email</label>
                <input name="email" type="email" class="form-control" id="field2" value="{{email}}">
            </div>
            {{!-- <div class="form-group">
                <label for="field2">Cambiar imagen</label>
                <input name="imageInput" type="file" class="form-control" id="field3" value="{{imageUrl}}">
                <small class="form-text text-muted">El tamaño de la imagen</small>

            </div> --}}

            <a class="btn btn-warning" href="/users/edit/changePassword/{{_id}}">Cambiar contraseña</a>


            <button type="submit" class="btn btn-dark btn-block btn-lg">Modificar los datos</button>

            {{#if errorMsg}}
            <div id="errorMsg">{{{errorMsg}}}</div>
            {{/if}}
        </form>


    </div>
</div>